// TODO: What will the following line of code log?
//Everything in the window object.
console.log(window);

// TODO: What will the following line of code log?
//the HTML that was loaded in the window object.
console.log(window.document);

// TODO: What will the following line of code log?
//The HTML elements (DOM) that were loaded in the window object.
console.log(document.documentElement);

// TODO: What will the following line of code log?
//The head elements that were loaded in the DOM of the window object.
console.log(document.head);

